# Changelog

## 1.0.0

- Initial pure-Dart Typhoon OCR core package.
- Includes typed document models, parsing, document definitions, structured validation, and shared exceptions.
- Extracted from `typhoon_ocr_flutter` while preserving the Flutter package public API through re-exports.
